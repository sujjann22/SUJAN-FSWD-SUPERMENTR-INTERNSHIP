# Assignment 23/03/2026 - Hello Server

A simple Node.js HTTP server with manual routing.

## Run

```bash
npm install
npm start
```

## Routes

- `GET /` - Home page
- `GET /about` - About page
- `GET /contact` - Contact page

## Notes

This project uses the built-in Node `http` module and demonstrates simple route handling without Express.
